import * as express from 'express'

declare global {
  namespace Express {
    // interface Locals {
    //   tokenData?: Record<strinf, unknown>
    // }
    // interface Response {
    //   locals?: Locals
    // }
  }
}
